# Cursor rollout exceptions

This generated context records the task-authorized module-overlay extension and progressive
rollout from Reva standards revision `740fdff52ba1d093d36652347f64850c51ce9700`.

- Root `AGENTS.md` is the only agent instruction file.
- Nested `.cursor` directories are bounded module overlays, explicitly indexed and hashed.
- Module overlays may contain rules, knowledge, and module-qualified skills only.
- Root owns organization rules, hooks, MCP, commands, custom agents, ignores, and safety policy.
- The standards release was still draft during this explicitly authorized rollout; generated
  files do not claim formal release attestation.
- The direct default-branch delivery authorization applies to this rollout only and is not a
  standing permission for future agents.
